const { chromium } = require('playwright');
const assert = require('assert');
(async()=>{
  const opts={headless:true}; if(process.env.KC_PLAYWRIGHT_CHANNEL)opts.channel=process.env.KC_PLAYWRIGHT_CHANNEL;
  const browser=await chromium.launch(opts);
  const context=await browser.newContext({viewport:{width:390,height:844},isMobile:true,hasTouch:true,deviceScaleFactor:2,locale:'de-DE'});
  const page=await context.newPage();
  const errors=[]; page.on('pageerror',e=>errors.push(e.message));
  await page.route('**/src/core/member-access.js*',async route=>{const response=await route.fetch();let body=await response.text();const marker="function configured(){const c=publicConfig();return /^https:\\/\\//.test(c.url)&&String(c.publishableKey||'').trim().length>20;}";if(!body.includes(marker))throw new Error('member-access marker missing');body=body.replace(marker,'function configured(){return false;}');await route.fulfill({response,body,headers:{...response.headers(),'content-type':'application/javascript; charset=utf-8'}})});
  await page.goto('http://127.0.0.1:4173/',{waitUntil:'domcontentloaded',timeout:15000});
  await page.waitForSelector('#uxLocalTest',{state:'attached',timeout:15000});
  const details=page.locator('#uxLocalTest').locator('xpath=ancestor::details'); if(await details.count())await details.locator('summary').click();
  await page.locator('#uxTestRole').selectOption('admin'); await page.locator('#uxTestLogin').click();
  await page.waitForSelector('#unlockSecret',{timeout:10000}); await page.locator('#unlockSecret').fill('KC-DP2-Mobile-Smoke-2026!'); await page.locator('#unlockBtn').click();
  await page.waitForSelector('#kcChoiceEdit',{state:'visible',timeout:20000});
  for(const id of ['#idbStatusLed','#idbTrafficLed','#supabaseStatusLed','#supabaseTrafficLed'])await page.waitForSelector(id,{state:'attached',timeout:3000});
  await page.locator('#kcChoiceEdit').click();
  await page.waitForFunction(()=>document.body.classList.contains('ux-legacy'),{timeout:10000});
  assert(await page.locator('#mainView').isVisible(),'mainView not visible after entering program');
  assert(await page.locator('#mainView .planner-wrap').isVisible(),'planner not rendered');
  await page.locator('#userBtn').click();
  await page.waitForSelector('#kcDiagnosticsAdminEntry',{state:'visible',timeout:4000});
  await page.locator('#kcDiagnosticsAdminEntry').click();
  await page.waitForSelector('#kcDiagOverlay',{state:'visible',timeout:5000});
  await page.waitForTimeout(250);
  assert(!errors.length,'page errors: '+errors.join(' | '));
  console.log('KC DP2 pre-email v0.19.54 reference: PASS');
  await browser.close();
})().catch(e=>{console.error('KC DP2 pre-email v0.19.54 reference: FAIL');console.error(e.stack||e);process.exit(1)});
