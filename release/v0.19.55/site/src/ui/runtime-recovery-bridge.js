(function(){
  'use strict';
  const K=window.KCDP=window.KCDP||{};
  let loading=false,diagScheduled=false;
  const byId=id=>document.getElementById(id);
  const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));

  function removeStaleMobileStatus(){byId('kcMobileDbStatus')?.remove()}
  function isSessionModal(){const m=byId('modal'),h=m?.querySelector('h2');return !!h&&/Anmeldung\s*\/\s*Monitor/i.test(h.textContent||'')}
  function hardClose(){byId('modalBackdrop')?.classList.add('hidden');const m=byId('modal');if(m){m.innerHTML='';m.classList.remove('wide')}document.body.classList.remove('modal-open');document.documentElement.classList.remove('modal-open')}
  function bindClose(el){if(!el||el.dataset.kcRecoveryClose==='1')return;el.dataset.kcRecoveryClose='1';const fn=e=>{e?.preventDefault?.();e?.stopPropagation?.();hardClose()};el.addEventListener('click',fn,true);el.addEventListener('pointerup',fn,true);el.addEventListener('touchend',fn,{capture:true,passive:false})}
  function ensureSessionClose(){const m=byId('modal');if(!m||!isSessionModal())return;const h=m.querySelector('h2');if(!h)return;let b=byId('kcSessionTopClose');if(!b){h.style.position='relative';h.style.paddingRight='60px';b=document.createElement('button');b.id='kcSessionTopClose';b.type='button';b.setAttribute('aria-label','Anmeldung / Monitor schließen');b.textContent='×';Object.assign(b.style,{position:'absolute',right:'0',top:'50%',transform:'translateY(-50%)',width:'48px',height:'48px',borderRadius:'50%',border:'1px solid #d8c9c1',background:'#fff',fontSize:'32px',lineHeight:'40px',zIndex:'10002',touchAction:'manipulation'});h.appendChild(b)}bindClose(b);bindClose(byId('sessionClose'))}
  function loadScript(id,src,timeout=5000){return new Promise((resolve,reject)=>{if(byId(id)){resolve();return}const s=document.createElement('script');s.id=id;s.src=src;s.async=false;const t=setTimeout(()=>{s.remove();reject(new Error('Modul-Ladevorgang hat nicht geantwortet.'))},timeout);s.onload=()=>{clearTimeout(t);resolve()};s.onerror=()=>{clearTimeout(t);s.remove();reject(new Error('Modul konnte nicht geladen werden.'))};document.head.appendChild(s)})}
  function fallback(message){let ov=byId('kcDiagRecoveryOverlay');if(!ov){ov=document.createElement('div');ov.id='kcDiagRecoveryOverlay';Object.assign(ov.style,{position:'fixed',inset:'0',zIndex:'180000',background:'rgba(0,0,0,.55)',display:'grid',placeItems:'center',padding:'18px'});ov.innerHTML='<section style="width:min(680px,96vw);max-height:90vh;overflow:auto;background:#fff;border-radius:18px;padding:20px;box-sizing:border-box;font-family:system-ui,Arial,sans-serif;box-shadow:0 18px 60px #0006"><div style="display:flex;justify-content:space-between;gap:12px;align-items:center"><h2 style="margin:0;color:#7a1420">🛠 Zentrale Fehlerdiagnose</h2><button id="kcDiagRecoveryClose" type="button" style="width:48px;height:48px;border-radius:50%;border:1px solid #ccc;background:#fff;font-size:30px">×</button></div><div id="kcDiagRecoveryBody" style="margin-top:14px;line-height:1.45"></div><button id="kcDiagRecoveryRetry" type="button" style="width:100%;min-height:48px;margin-top:14px">Erneut versuchen</button></section>';document.body.appendChild(ov);byId('kcDiagRecoveryClose').onclick=()=>ov.remove();byId('kcDiagRecoveryRetry').onclick=()=>openDiagnostics(true)}byId('kcDiagRecoveryBody').innerHTML=`<p><b>Diagnose konnte noch nicht geöffnet werden.</b></p><p>${esc(message||'Unbekannter Fehler')}</p><p style="font-size:.9em;color:#555">Diagnose-Modul: ${K.diagnostics?'geladen':'fehlt'} · Diagnose-UI: ${K.diagnosticsCenter?'geladen':'fehlt'} · Rolle: ${esc(K.currentUser?.role||'unbekannt')}</p>`}
  function loadingOverlay(){let ov=byId('kcDiagRecoveryOverlay');if(!ov){fallback('Diagnose wird geöffnet …');ov=byId('kcDiagRecoveryOverlay')}const body=byId('kcDiagRecoveryBody');if(body)body.innerHTML='<p><b>Diagnose wird geöffnet …</b></p><p>Die Oberfläche wird sofort geöffnet. Die Daten laden danach im Hintergrund.</p>';return ov}
  async function ensureModules(){if(!K.diagnostics)await loadScript('kcRecoveryDiagnosticsAdapter','src/adapters/diagnostics.js?v=0.19.64-recovery1');if(!K.diagnosticsCenter)await loadScript('kcRecoveryDiagnosticsCenter','src/ui/diagnostics-center.js?v=0.19.64-nonblocking1');if(!K.diagnostics)throw new Error('Diagnose-Adapter ist nach dem Nachladen nicht verfügbar.');if(!K.diagnosticsCenter?.open)throw new Error('Diagnose-Oberfläche ist nach dem Nachladen nicht verfügbar.')}
  async function openDiagnostics(force=false){if(loading&&!force)return;loading=true;try{removeStaleMobileStatus();if(!byId('kcDiagRecoveryOverlay'))loadingOverlay();hardClose();await ensureModules();const result=K.diagnosticsCenter.open();if(result?.opened===false)throw new Error(result.reason==='not-allowed-or-diagnostics-missing'?'Diagnose ist für diese Rolle oder Sitzung noch nicht verfügbar.':'Diagnose konnte nicht geöffnet werden.');if(byId('kcDiagOverlay'))byId('kcDiagRecoveryOverlay')?.remove();else setTimeout(()=>{if(!byId('kcDiagOverlay'))fallback('Die Diagnosefunktion wurde aufgerufen, aber das Diagnosefenster ist nicht sichtbar geworden.')},150)}catch(e){fallback(e?.message||String(e))}finally{loading=false}}
  function scheduleDiagnostics(e){e?.preventDefault?.();e?.stopPropagation?.();e?.stopImmediatePropagation?.();loadingOverlay();if(diagScheduled)return;diagScheduled=true;setTimeout(()=>{diagScheduled=false;void openDiagnostics()},0)}
  function isDiagButton(target){const b=target?.closest?.('button');return !!b&&(b.id==='kcDiagnosticsAdminEntry'||/Zentrale\s+Fehlerdiagnose/i.test(b.textContent||''))}
  function bindDiag(el){if(!el||el.dataset.kcRecoveryDiag==='1')return;el.dataset.kcRecoveryDiag='1';el.style.touchAction='manipulation';el.addEventListener('pointerdown',scheduleDiagnostics,true);el.onclick=scheduleDiagnostics}
  document.addEventListener('pointerdown',e=>{if(isDiagButton(e.target))scheduleDiagnostics(e)},true);
  document.addEventListener('click',e=>{if(isDiagButton(e.target))scheduleDiagnostics(e)},true);
  function apply(){removeStaleMobileStatus();ensureSessionClose();document.querySelectorAll('button').forEach(b=>{if(isDiagButton(b))bindDiag(b)})}
  apply();const obs=new MutationObserver(apply);if(document.documentElement)obs.observe(document.documentElement,{childList:true,subtree:true});
  K.runtimeRecoveryBridge={version:'0.19.64-pointerdown4',openDiagnostics,removeStaleMobileStatus,ensureSessionClose,hardClose,apply};
})();
